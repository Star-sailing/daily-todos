-- ============================================================
-- Daily Todos — 完成率按天统计 + 顺延链"关闭"标记
-- 在 Supabase SQL Editor 中运行（幂等，可重复执行）
-- ============================================================

-- 根源行的"完成日期"：任务在将来某天被勾掉时，只把当天那条标为完成，
-- 同时在根源行记下完成日期（用来停止继续顺延）；过去几天的历史副本
-- 保持"未完成"，这样完成率才能精确到"当天有没有完成"。
ALTER TABLE todos ADD COLUMN IF NOT EXISTS completed_date DATE;
